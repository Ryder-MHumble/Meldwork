# Verification And Test Coverage

Verification evidence below spans 2026-07-29 through 2026-08-05 on macOS arm64. Repository tooling and CI support Node.js 20.19 or newer; the packaged desktop runs on Electron's bundled Node.js runtime. On 2026-08-05, the Harness runtime working tree completed the current frontend and desktop suites, both renderer builds, arm64 package validation, and strict ad-hoc signature verification. Unchanged distribution, smoke, compatibility, and audit rows retain their earlier executed baseline.

## Executed Verification

| Check | Evidence | Result |
| --- | --- | --- |
| Frontend unit suite | `npm --prefix frontend test` | 30 files and 233/233 tests passed on 2026-08-05 |
| Desktop unit suite | `npm --prefix desktop test` | 826/826 tests passed on 2026-08-05 |
| CI Node adapter compatibility | `npx -y node@20.19.0 --test desktop/test/cli-adapters.test.cjs` | 71/71 tests passed |
| Renderer builds | `npm --prefix frontend run build` and `npm --prefix frontend run build:desktop` | Both Vite builds passed on 2026-08-05; the existing chunk-size warning remains non-fatal |
| Current desktop package validation | `npm --prefix desktop run pack` and `codesign --verify --deep --strict desktop/dist/mac-arm64/Meldwork.app` | Electron 39.8.5 arm64 packaging passed on 2026-08-05. The exact bundle is `com.roundrelay.desktop` version `0.1.0`; `app.asar` SHA-256 is `82f7a617725a235025876615ee2427ec5b252707c0260001140c06ed59e678da`. Strict verification passed with an ad-hoc signature. This is not Developer ID signing or notarization |
| Harness / Run Ledger automated regression coverage | `frontend/src/__tests__/roundrelay/App.conversation-trace.spec.js`; `frontend/src/__tests__/roundrelay/App.run-trace.spec.js`; `desktop/test/run-harness.test.cjs`; `desktop/test/run-ledger.test.cjs`; `desktop/test/local-workspace-harness.test.cjs` | Included in the passing 2026-08-05 suites: direct inline and group-panel trace contracts, streamed terminal conclusions, compact evidence handoff, sanitized checkpoints, provenance conflict rejection, durable restart recovery, no overall Run deadline, per-Agent watchdog cleanup, retryable transactions, and stale run-ID stop rejection |
| Runtime interruption and Human Gate recovery | `desktop/test/local-workspace-human-gate-recovery.test.cjs`; `desktop/test/human-gate-coordinator.test.cjs`; `desktop/test/local-workspace-run-coordinator.test.cjs`; `desktop/test/local-workspace-manual-controls.test.cjs`; `desktop/test/local-workspace.test.cjs` | Replay-safe final Agent slots can recover; legacy Role Review continuations fail closed with `LOCAL_WORKFLOW_UNSUPPORTED`, and non-final multi-Agent slots without a durable orchestration cursor fail closed with `LOCAL_RUN_ORCHESTRATION_RESUME_UNAVAILABLE`. Initial or resume checkpoint failures roll pending continuation state back, parallel Gates are exposed serially, waiter/listener cleanup is bounded, and shutdown still settles when a waiting checkpoint fails |
| Agent, Knowledge, and mock Cloud runtime coverage | `desktop/test/agent-connector-manifest.test.cjs`; `desktop/test/agent-connector-registry.test.cjs`; `desktop/test/agent-connector-instance-store.test.cjs`; `desktop/test/agent-connector-runtime.test.cjs`; `desktop/test/run-event-protocol.test.cjs`; `desktop/test/knowledge-connector-registry.test.cjs`; `desktop/test/local-knowledge-connectors.test.cjs`; `desktop/test/cloud-agent-runtime.test.cjs` | Approved Agent Manifests/instances, two Knowledge implementations, mock asynchronous Cloud execution, Human Gates, and fail-fast 401/403 handling that retains the configured Agent are included in the passing desktop suite. Production Channel and Cloud connectors are not configured |
| Electron Hermes SQLite smoke | Temporary Hermes-shaped `state.db` queried through Electron with `ELECTRON_RUN_AS_NODE=1` | Electron 39.8.5 / Node.js 22.22.1 loaded `node:sqlite`; the watermark excluded the previous turn and `tool_calls`, a post-watermark `length` result was accepted, and a later `stop` result took precedence |
| macOS distribution | `npm --prefix desktop run dist` | arm64 `.app`, DMG, and ZIP produced |
| Production dependency audit | `npm --prefix desktop audit --omit=dev` and `npm --prefix frontend audit --omit=dev` | 0 known vulnerabilities in both production dependency sets |
| ACP runtime packaging | Inspect and extract packaged `app.asar`, then dynamically import its production modules | `@agentclientprotocol/sdk` 0.23.0, `zod` 4.4.3, the ACP client, and both inbound Zod validators loaded successfully |
| Current packaged startup and quit smoke | Launch the exact `desktop/dist/mac-arm64/Meldwork.app` with fresh profile `/tmp/meldwork-role-review-smoke.R6owYX` and CDP port `9585` | The packaged page reached `complete`, rendered one populated root with 1,143 visible-text characters and 30 visible buttons, contained no Role Review text, entry, or form, and had no horizontal overflow. Closing through the browser debugging endpoint removed the app process and CDP listener |
| Packaged Harness group acceptance (pre-final edge-fix package) | Launch the packaged app with isolated profile `/private/tmp/meldwork-final-harness-profile.xVSai5`; inspect group `efcf61d5-2875-419d-a285-0c7b01171d8d`, run `ccae01a8-89c0-4088-9da2-ff8b77cec9b1`, and Task/thread root `7c25e3f7-aa88-47cb-a87f-daa6b5aa12c6` | Two rounds completed with no failures or 401 retries. Hermes and Codex both retained `turns: 2`; Hermes used one Task-scoped `legacy` Session with complete provenance. Live preload capture observed 14 events including four `answer_delta` events; the group showed no inline process details and opened a 380 CSS px right-side panel. Hermes truthfully reported `connector_limited` where legacy transport could not expose structured tool details. Ledger state retained the compacted durable trace rather than every live event |
| Historical packaged Harness direct acceptance | In the earlier exact packaged app/profile, inspect direct conversation `e85c04a5-6029-4c68-91aa-2696b759f64a` during and after a real Codex run | While running, conclusion and tool/status details streamed inline with no right panel. After completion, marker `DIRECT_LIVE_CAPTURE_OK` remained, execution details collapsed with two durable tool events, and no right panel appeared. The app then quit cleanly and both its PID and CDP listener were gone |
| Logo packaging | Compare source, Vite output, and packaged asar hashes; inspect generated `icon.icns` and packaged UI | The final 3D Relay Fold app/product PNGs match source byte-for-byte in the package, and the packaged renderer loads the 1024x1024 brand asset |
| Idle startup network | Inspect the Meldwork process tree with `lsof` after disconnecting CDP | No established TCP or UDP connection; only the explicitly enabled loopback CDP listener remained |
| Empty-store secure-storage behavior | Unit tests plus a one-second process sample | No `SecItemCopyMatching`, `SecKeychain`, `CSSM_Decrypt`, or decrypt call observed without a stored Provider |
| macOS signature | `codesign --verify --deep --strict` | Passed with ad-hoc signature; `spctl` rejection is expected until Developer ID signing/notarization exists |

The broader first-run, group creation, Skill IPC, clipboard attachment, real Hermes/Codex group relay, and direct-stream acceptance remain executed historical baselines. After the final shutdown, Gate, direct-401, and parallel-failure fixes, the exact current package was rebuilt and passed the startup/preload/render/quit smoke above; the final edge fixes are additionally covered by focused and full desktop regressions, but the current package did not repeat the complete live Agent CLI matrix.

## Automated Coverage

| Use case / rule | Expected behavior, including deny case | Evidence | Status |
| --- | --- | --- | --- |
| Renderer workbench and onboarding | Shows the configured Agent catalog, gates the first-run carousel on Agent detection, supports focus/scroll/back dismissal, local groups/direct chats, theme, and locale | `frontend/src/__tests__/roundrelay/App.spec.js` | Passed locally |
| Conversation run UX and session management | Shows Agent logos and capabilities, multiple direct sessions, rename/delete controls, per-Agent queued/running/completed state, automatic current/max round progress, inline direct execution, group trace-panel disclosure, background direct completion markers, unavailable-Agent history access, and truthful permission/image limits | `frontend/src/__tests__/roundrelay/App.spec.js`; `frontend/src/__tests__/roundrelay/App.conversation-trace.spec.js`; `frontend/src/__tests__/roundrelay/App.run-trace.spec.js`; `desktop/test/main-security.test.cjs` | Passed locally |
| Harness trace and Run Ledger durability | Streams allowlisted per-Agent trace and live context state, retains compact completed/partial/failed/stopped/timeout/interrupted traces, visibly renders streamed terminal conclusions, hands later Agents bounded source-addressable evidence, checkpoints sanitized run state, fails closed on malformed values, provenance conflicts, and duplicate identities, reconstructs durable turn outcomes after restart, keeps mutations retryable, reconciles terminal checkpoints idempotently, rejects stale run-ID stop requests, and purges ledger records with deleted conversations | `frontend/src/__tests__/roundrelay/App.run-trace.spec.js`; `desktop/test/run-harness.test.cjs`; `desktop/test/run-ledger.test.cjs`; `desktop/test/local-workspace-harness.test.cjs` | Passed locally |
| Immutable Context Packs and Session provenance | Captures immutable message, Skill, Knowledge, and attachment inputs; compares exact outbound payloads after mutation/restart; scopes native Sessions to Tasks; records inherited ancestry and provenance limits; warns instead of presenting newly packed IDs as complete latent Session context | `desktop/test/context-pack-store.test.cjs`; `desktop/test/local-workspace-context-packs.test.cjs`; `desktop/test/local-skill-snapshot.test.cjs`; `frontend/src/__tests__/roundrelay/App.run-trace.spec.js` | Passed locally |
| Human Gate, budget, and continuation recovery | Permission, budget, and generic decisions are durable; only replay-safe final Agent slots resume after restart; legacy Role Review continuations, non-final multi-Agent slots, cross-Run continuations, and missing Packs fail closed; checkpoint failure restores pending continuation state; parallel Gates serialize; manual retry/cancel/replace remain scoped to one Agent | `desktop/test/human-gate-coordinator.test.cjs`; `desktop/test/local-workspace-human-gate-recovery.test.cjs`; `desktop/test/local-workspace-run-coordinator.test.cjs`; `desktop/test/local-workspace-manual-controls.test.cjs`; `desktop/test/failure-policy.test.cjs`; `desktop/test/run-budget.test.cjs`; `desktop/test/run-scheduler.test.cjs` | Passed locally |
| Agent Connector contract | Enforces approved immutable Manifests/recipes, scoped instances, encrypted CredentialRefs, external discovery without core allowlist edits, version provenance, idempotent Run Event reduction, main/preload boundaries, and restart-stable replay | `desktop/test/agent-connector-manifest.test.cjs`; `desktop/test/agent-connector-registry.test.cjs`; `desktop/test/agent-connector-instance-store.test.cjs`; `desktop/test/agent-connector-runtime.test.cjs`; `desktop/test/run-event-protocol.test.cjs`; `desktop/test/main-security.test.cjs`; `desktop/test/preload-security.test.cjs` | Passed locally |
| Knowledge Connector contract | Enforces the full authorize/revoke/probe/search/fetch/snapshot/citation lifecycle across filesystem and memory implementations, stable bounded source IDs, connector-enforced read-only access, immutable snapshots, explicit live references, and citation reopening after restart | `desktop/test/knowledge-connector-contract.test.cjs`; `desktop/test/knowledge-connector-filesystem.test.cjs`; `desktop/test/knowledge-connector-memory.test.cjs`; `desktop/test/knowledge-connector-registry.test.cjs`; `desktop/test/local-knowledge-connectors.test.cjs`; `desktop/test/main-security.test.cjs`; `desktop/test/preload-security.test.cjs` | Passed locally |
| Mock Cloud Agent contract | Supports asynchronous submit/observe/input/cancel/artifact/reconcile, durable job/cursor replay, UI detach/reattach, duplicate suppression, cancel-once behavior, bounded shutdown, and retryable startup after recovery failures without requiring a server for local-only use | `desktop/test/cloud-agent-contract.test.cjs`; `desktop/test/cloud-agent-operation-store.test.cjs`; `desktop/test/cloud-agent-runtime.test.cjs`; `desktop/test/main-security.test.cjs`; `desktop/test/preload-security.test.cjs` | Passed locally; no production Cloud connector configured |
| Channel ingress framework and outbound audit | Test connectors verify signatures, reject stale/replayed/cross-scope events, map one durable Delivery to one Task through an injected `taskSink`, preserve cursor order, and replay outbound replies idempotently without renderer ownership. Production bootstrap intentionally has `connectors: []` and no `taskSink`, so no task-oriented Channel is shipped | `desktop/test/channel-ingress-contract.test.cjs`; `desktop/test/channel-inbox-store.test.cjs`; `desktop/test/channel-ingress-runtime.test.cjs`; `desktop/test/main-security.test.cjs` | Framework passed locally; production Channel not configured |
| Composer Skill routing | Loads Skills only for selected targets, supports `@` selection, and sends structured target-scoped coordinates | `frontend/src/__tests__/roundrelay/App.spec.js`; `desktop/test/local-skill-catalog.test.cjs`; `desktop/test/agent-installer.test.cjs`; `desktop/test/local-workspace.test.cjs` | Passed locally |
| Legacy knowledge-base hint routing | Separates ready/planned Feishu, DingTalk, and Obsidian hints, requires readable access, scopes selections to target Agents, persists only the selected Vault path, and rejects stale or unauthorized hints | `frontend/src/__tests__/roundrelay/App.spec.js`; `desktop/test/local-knowledge-base.test.cjs`; `desktop/test/knowledge-base-store.test.cjs`; `desktop/test/local-workspace.test.cjs`; `desktop/test/main-security.test.cjs` | Passed locally |
| Image attachment UI | Imports picker/paste images, permits image-only messages, blocks send and concurrent paste batches during import, keeps accepted all-failed messages out of the composer, discards unsent/overflow/stale imports, and sends metadata without paths or preview data | `frontend/src/__tests__/roundrelay/App.spec.js` | Passed locally |
| Attachment preview cache | Loads visible previews only, limits concurrent main-process requests to two, and evicts non-visible least-recent entries from the bounded cache | `frontend/src/__tests__/roundrelay/attachmentPreviews.spec.js` | Passed locally |
| Renderer bridge normalization | Uses only `window.roundrelayDesktop` services and normalizes missing data | `frontend/src/__tests__/roundrelay/desktop.spec.js` | Passed locally |
| Renderer i18n | Chinese/English keys and interpolation remain consistent | `frontend/src/__tests__/roundrelay/i18n.spec.js` | Passed locally |
| Renderer CSP and assets | Entry point keeps self-only/default restrictions, denies unsafe surfaces, and uses relative packaged assets | `frontend/src/__tests__/roundrelay/security.spec.js` | Passed locally |
| Main-frame IPC authorization | All registered local channels accept only the exact frontend main frame; other frames/paths are denied before dispatch | `desktop/test/main-security.test.cjs` | Passed locally |
| Electron navigation, lifecycle, and shutdown gate | Loads bundled frontend, denies in-window remote navigation, serializes refresh, rejects every trusted IPC call after shutdown starts, then waits for workspace/installer cleanup before quitting | `desktop/test/main-security.test.cjs` | Passed locally |
| Narrow preload API | Local documents get only named workspace, installer, Skill snapshot, attachment, Provider, local Agent Connector, Knowledge lifecycle, Cloud continuation/cancel, Outcome/Adoption, Human Gate, and per-Agent control methods. Attachment APIs expose no file read or path resolution; non-local documents get no privileged API | `desktop/test/preload-security.test.cjs` | Passed locally |
| Packaged Electron hardening | Fuses disable Node/runtime injection and require ASAR integrity | `desktop/test/package-security.test.cjs` | Passed locally |
| Provider secret storage | Complete payload required; URL constrained; key encrypted; failures/corruption fail closed; delete works | `desktop/test/provider-store.test.cjs` | Passed locally |
| Agent readiness | Detects native credentials without returning values and scopes forwarded credential variables by Agent | `desktop/test/local-agent-readiness.test.cjs` | Passed locally |
| Installer allowlist and lifecycle | Fixed recipes, URL/command rejection, environment filtering, verification, cancellation, and single-task behavior | `desktop/test/agent-installer.test.cjs` | Passed locally |
| Image storage and validation | Validates PNG/JPEG signatures, MIME/name agreement, 8 MiB/four-image limits, image dimensions, private storage, checksums, tamper resistance, cleanup, bounded previews, and referenced-attachment deletion guards | `desktop/test/attachment-store.test.cjs`; `desktop/test/image-dimensions.test.cjs`; `desktop/test/main-security.test.cjs` | Passed locally |
| CLI invocation and Hermes final-result safety | Per-Agent read-only/write arguments, Kimi ACP lifecycle, image limits, Hermes native Skill preload, guarded ACP-to-legacy fallback, invalid-session classification, best-effort pre-run message watermark, post-watermark `assistant` `stop`/`length` selection, official stdout fallback, early session persistence, environment scoping, secret redaction, and process-tree cancellation | `desktop/test/cli-adapters.test.cjs`; `desktop/test/cli-adapters-agent-protocols.test.cjs` | Passed locally |
| Workspace state and automation | Explicit persisted-field allowlists, path stripping, visual Task roots, Task-and-Agent group sessions, conversation-and-Agent direct sessions, exact legacy migration, workdir invalidation, bounded shared conclusions, target-scoped Skills, equal attachment context, cancellation-aware preflight, final-only downstream relay, failure isolation, complete rounds, strict consensus, no overall Run deadline, and per-Agent watchdog bounds | `desktop/test/local-workspace.test.cjs`; `desktop/test/local-workspace-auto.test.cjs`; `desktop/test/local-workspace-harness.test.cjs`; `desktop/test/run-scheduler.test.cjs` | Passed locally |
| Terminal run lifecycle | Completion events are transient and best-effort, listener failures do not change results, all-failed automatic runs keep the round-limit diagnostic, notification payloads are content-free, notification clicks open the target conversation, background successful direct runs receive completion markers, and shutdown suppresses new notifications | `desktop/test/local-workspace.test.cjs`; `desktop/test/main-security.test.cjs` | Passed locally |
| Managed OpenClaw | Isolated paths, key absent from config, immutable permission config, invalid scopes fail closed | `desktop/test/openclaw-runtime.test.cjs` | Passed locally |
| macOS packaging transform | Removes unused permission declarations, sets Electron fuses, and applies ad-hoc signing | `desktop/test/after-pack.test.cjs` | Passed locally |

## CI-Declared Checks

`.github/workflows/ci.yml` declares these push and pull-request checks:

- Frontend: `npm ci`, `npm test`, `npm run build`, `npm run build:desktop` on Ubuntu.
- Desktop: frontend/desktop `npm ci`, `npm test`, and `npm run pack` on macOS.

The workflow file exists, but branch protection and successful required checks are external GitHub state and are not verified by this repository documentation.

## Proposed Tests

| Type | Use case / rule | Expected behavior | Status |
| --- | --- | --- | --- |
| Automated packaged-app IPC integration | Extend the focused packaged smoke to exercise real preload IPC and attachment/Skill flows | Privileged calls work only in the main frame; imported images and selected Skills reach the expected Agent without exposing paths | Proposed |
| Automated Connector end-to-end integration | Drive one external Connector event through main-process validation, durable reduction, preload normalization, and renderer state | Supported fields and terminal state survive the complete pipeline and restart without duplicate effects | Proposed |
| Guarded live | One production task-oriented Channel Connector with an injected Task sink | Authenticated deliveries map idempotently to local Tasks and approved replies replay without renderer ownership | Proposed |
| Guarded live | One real Cloud Agent provider | Remote submit, detach/reattach, input, cancel, Artifact fetch, and reconciliation match the mock contract | Proposed |
| Guarded live | Real Agent CLI matrix on supported macOS/Windows versions | Detection, readiness, prompt execution, cancellation, session resume, Hermes final-row timing, and permission behavior match unit contracts | Proposed |
| Guarded live | Installer recipes on clean disposable OS users/VMs | Only the selected Agent is installed, cancellation cleans up, secrets are absent from child environment | Proposed |
| Automated visual regression | Light/dark theme and logo contrast | Detect theme, layout, or asset regressions without relying on manual screenshot inspection | Proposed |
| Guarded live | macOS Developer ID signing and notarization | Gatekeeper accepts the app and DMG on a clean machine | Proposed |
| Guarded live | Windows packaged build | Installation, app launch, CLI discovery, npm shim handling, and cancellation work on a real Windows device | Proposed |
| Automated resilience | Abrupt termination during workspace/provider writes | Previous valid state remains readable and no secret is left in a temporary file | Proposed |

## Gaps

| Priority | Rule with no current verification | Exposure | Status |
| --- | --- | --- | --- |
| High | Public release signing/notarization is not exercised | Distributed macOS artifacts can be rejected by Gatekeeper | None |
| High | No concrete production Channel Connector or Task sink is configured | The Channel framework cannot yet receive a real external Task or deliver a production reply | Test Connector/taskSink coverage only; Epic #14 and Issue #5 remain open |
| Medium | No real Cloud Agent provider acceptance | Provider-specific authentication, polling, callback, and Artifact semantics may diverge from the mock contract | Mock Cloud Connector coverage only |
| Medium | No single automated Connector -> main -> persistence -> preload -> renderer test | Individually tested boundaries may still disagree when composed | Contract, main/preload, persistence, and renderer tests exist separately |
| Medium | No automated packaged preload/Agent end-to-end suite | Unit mocks may miss real IPC, attachment/Skill routing, or Agent process integration faults | Historical guarded live Harness/direct acceptance plus a current packaged startup/quit smoke; no automated suite |
| Medium | No clean-machine live Agent/installer matrix | Upstream CLI changes can invalidate flags, output parsing, paths, or installer behavior | None |
| Medium | No repeatable live Hermes CLI matrix, current-package direct run, or real macOS notification-click exercise | Upstream Hermes authentication/persistence timing and operating-system notification behavior can diverge from the isolated acceptance run | One isolated two-round packaged Hermes-to-Codex run, historical direct acceptance, and Electron SQLite smoke; no repeatable matrix |
| Medium | Windows release is not built or tested in CI | Windows support remains implementation-level rather than release-verified | None |
| Low | No automated visual regression or accessibility audit | Theme, contrast, focus, and responsive regressions require manual discovery | None |
| Low | No long-history/performance test | Large local message histories may expose latency or file-size limits late | None |
